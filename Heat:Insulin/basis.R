dlm_basis <- function(X, lag, )
{
  out <- list()
  class(out) <- "dlm_basis"
}

dlint_basis <- function(X1, X2, lag1, lag2, )
{
  out <- list()
  class(out) <- "dlm_basis"
}

dl_pen <- function(dl_basis)
{
  out <- list()
  class(out) <- "dl_pen"
}
